"""Vendored, Blender-free dependencies of the embedded client.

- ``pyoc`` — the Python face of the OmnibusCloud native SDK, copied verbatim from
  ``Client/python/pyoc`` in the WitCloud repository (keep in sync with the native
  library version shipped in this package).
- ``render_documents`` — the generated render vocabulary binding, copied
  verbatim from ``Render/OutWit.Controller.Render.Model/Documents`` in the
  Controllers repository (regenerated by OutWit.Cloud.Documents.Generator on
  every model build; a change here means the Render.Model package changed).

Nothing in this folder is edited by hand.
"""
