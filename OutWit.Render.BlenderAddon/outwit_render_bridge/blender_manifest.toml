schema_version = "1.0.0"
id = "omnibuscloud_render_bridge"
version = "0.14.1"
name = "OmnibusCloud Render Bridge"
tagline = "Thin Blender addon for the local OmnibusCloud render bridge"
maintainer = "OutWit"
type = "add-on"
website = "https://omnibuscloud.com"
blender_version_min = "4.0.0"
license = ["SPDX:MIT"]
tags = ["Render"]

[permissions]
files = "Upload .blend files, download render results, and access local bridge session/cache files."
network = "Communicate with the local OmnibusCloud bridge and the configured OmnibusCloud services."
